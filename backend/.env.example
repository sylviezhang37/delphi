# Example environment variables for Delphi backend (redacted)
# Copy this file to `.env` and fill with your real secrets locally.
# Do NOT commit your real `.env` file to git.

GOOGLE_API_KEY=your_google_api_key_here
